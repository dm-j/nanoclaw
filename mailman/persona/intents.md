# Triage Intents

Active instructions from the main agent that override default triage behavior.
These apply to ALL feeds. For per-feed rules, use the feed's `prompt.md`.

<!-- The main agent edits this file. Each entry should be a clear directive. -->
